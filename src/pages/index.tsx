import Head from 'next/head'
import React from 'react'
import {useSelector} from 'react-redux'
import {RootStore} from '~src/store/RootStore'
import Navbar from '~src/components/Navbar'
import TransitionShow from '~src/components/utils/TransitionShow'
import CardQuickSendGas from '~src/components/cards/CardQuickSendGas'
import CardCandidate from '~src/components/cards/CardCandidate'
import CardVotesStats from '~src/components/cards/CardVotesStats'
import CardRegisteredCandidateStats from '~src/components/cards/CardRegisteredCandidateStats'
import CardCommittee from '~src/components/cards/CardCommittee'

export default function Home() {
  const {isConnected} = useSelector(RootStore.auth.getters)

  return (
    <>
      <Head>
        <title>Neo Voting</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <Navbar />

      <div className={'typo min-h-screen-no-nav'}>
        <main className={'container'}>
          <TransitionShow if={isConnected}>
            <CardQuickSendGas className={'mb-4'} />

            <CardCandidate className={'mb-4'} />
          </TransitionShow>

          <div className={'mb-4 flex flex-col md:flex-row'}>
            <CardVotesStats className={'mb-4 md:mb-0 md:mr-4 flex-1'} />

            <CardCommittee className={'flex-1'} />
          </div>

          <CardRegisteredCandidateStats className={'-mx-4'} />
        </main>
      </div>
    </>
  )
}
